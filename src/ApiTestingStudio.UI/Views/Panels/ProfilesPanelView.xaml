<UserControl x:Class="ApiTestingStudio.UI.Views.Panels.ProfilesPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <TabControl Margin="{StaticResource Spacing.Xs.Thickness}">
        <TabItem Header="Profiles">
            <DockPanel LastChildFill="True">
                <ToolBarTray DockPanel.Dock="Top">
                    <ToolBar>
                        <Button Content="New" Command="{Binding NewProfileCommand}" ToolTip="Create a new profile" />
                        <Button Content="Edit" Command="{Binding EditProfileCommand}" ToolTip="Edit the selected profile" />
                        <Button Content="Delete" Command="{Binding DeleteProfileCommand}" ToolTip="Delete the selected profile" />
                    </ToolBar>
                </ToolBarTray>
                <ListBox ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         DisplayMemberPath="Name"
                         Margin="{StaticResource Spacing.Xs.Thickness}"
                         AutomationProperties.Name="Profiles" />
            </DockPanel>
        </TabItem>

        <TabItem Header="Environments">
            <DockPanel LastChildFill="True">
                <ToolBarTray DockPanel.Dock="Top">
                    <ToolBar>
                        <Button Content="New" Command="{Binding NewEnvironmentCommand}" ToolTip="Create a new environment" />
                        <Button Content="Edit" Command="{Binding EditEnvironmentCommand}" ToolTip="Edit the selected environment" />
                        <Button Content="Delete" Command="{Binding DeleteEnvironmentCommand}" ToolTip="Delete the selected environment" />
                    </ToolBar>
                </ToolBarTray>
                <ListBox ItemsSource="{Binding Environments}"
                         SelectedItem="{Binding SelectedEnvironment}"
                         DisplayMemberPath="Name"
                         Margin="{StaticResource Spacing.Xs.Thickness}"
                         AutomationProperties.Name="Environments" />
            </DockPanel>
        </TabItem>

        <TabItem Header="Variables">
            <DockPanel LastChildFill="True">
                <ToolBarTray DockPanel.Dock="Top">
                    <ToolBar>
                        <Button Content="New" Command="{Binding NewVariableCommand}" ToolTip="Create a new variable" />
                        <Button Content="Edit" Command="{Binding EditVariableCommand}" ToolTip="Edit the selected variable" />
                        <Button Content="Delete" Command="{Binding DeleteVariableCommand}" ToolTip="Delete the selected variable" />
                    </ToolBar>
                </ToolBarTray>
                <ListBox ItemsSource="{Binding Variables}"
                         SelectedItem="{Binding SelectedVariable}"
                         Margin="{StaticResource Spacing.Xs.Thickness}"
                         AutomationProperties.Name="Variables">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Key}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Scope}" Opacity="0.7" Margin="{StaticResource Spacing.Sm.LeftThickness}" />
                                <TextBlock Text="🔒" Margin="{StaticResource Spacing.Sm.LeftThickness}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSecret}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </TabItem>
    </TabControl>
</UserControl>
