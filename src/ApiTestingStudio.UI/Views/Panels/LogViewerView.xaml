<UserControl x:Class="ApiTestingStudio.UI.Views.Panels.LogViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <!-- Filter bar -->
        <Grid DockPanel.Dock="Top" Margin="{StaticResource Spacing.Sm.Thickness}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Level" VerticalAlignment="Center" Margin="{StaticResource Spacing.Sm.RightThickness}" />
            <ComboBox Grid.Column="1"
                      Width="120"
                      Margin="{StaticResource Spacing.Md.RightThickness}"
                      ItemsSource="{Binding LevelOptions}"
                      SelectedItem="{Binding SelectedLevel}"
                      AutomationProperties.Name="Log level filter" />

            <TextBlock Grid.Column="2" Text="Source" VerticalAlignment="Center" Margin="{StaticResource Spacing.Sm.RightThickness}" />
            <ComboBox Grid.Column="3"
                      Width="200"
                      Margin="{StaticResource Spacing.Md.RightThickness}"
                      ItemsSource="{Binding SourceOptions}"
                      SelectedItem="{Binding SelectedSource}"
                      AutomationProperties.Name="Log source filter" />

            <TextBox Grid.Column="4"
                     Margin="{StaticResource Spacing.Md.RightThickness}"
                     VerticalContentAlignment="Center"
                     AutomationProperties.Name="Search log messages"
                     ToolTip="Search log messages"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=250}" />

            <Button Grid.Column="5"
                    Content="Refresh"
                                       Command="{Binding RefreshCommand}" />
        </Grid>

        <!-- Empty state -->
        <TextBlock DockPanel.Dock="Top"
                   Margin="{StaticResource Spacing.Md.Thickness}"
                   Opacity="0.7"
                   Text="No log events match the current filters."
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}" />

        <DataGrid ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  AutomationProperties.Name="Log events"
                  Margin="{StaticResource Spacing.Sm.Thickness}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding TimestampUtc, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                    Width="150" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="90" />
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="220" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
