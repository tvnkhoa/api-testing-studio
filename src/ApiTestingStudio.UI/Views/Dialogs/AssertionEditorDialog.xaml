<Window x:Class="ApiTestingStudio.UI.Views.Dialogs.AssertionEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="16">
        <TextBlock Text="Type" />
        <ComboBox ItemsSource="{Binding Kinds}" SelectedItem="{Binding SelectedKind}" Margin="0,4,0,0" />

        <TextBlock Text="Source (response field)" Margin="0,12,0,0" />
        <ComboBox ItemsSource="{Binding Sources}" SelectedItem="{Binding Source}" Margin="0,4,0,0" />

        <TextBlock Text="Target (header name — for the Header source)" Margin="0,12,0,0" />
        <TextBox Text="{Binding Target, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />

        <TextBlock Text="Expression (JSONPath for json; regex pattern for regex)" Margin="0,12,0,0" />
        <TextBox Text="{Binding Expression, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />

        <TextBlock Text="Operator (json: equals / contains / exists / gt / lt …)" Margin="0,12,0,0" />
        <TextBox Text="{Binding Operator, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />

        <TextBlock Text="Expected (value, or the JSON Schema for the schema type)" Margin="0,12,0,0" />
        <TextBox Text="{Binding Expected, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60" VerticalScrollBarVisibility="Auto"
                 Margin="0,4,0,0" />

        <CheckBox Content="Enabled" IsChecked="{Binding Enabled}" Margin="0,12,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" MinWidth="80" Margin="0,0,8,0" />
            <Button Content="OK" IsDefault="True" MinWidth="80" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" />
        </StackPanel>
    </StackPanel>
</Window>
