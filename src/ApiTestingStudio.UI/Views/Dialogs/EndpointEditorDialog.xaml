<Window x:Class="ApiTestingStudio.UI.Views.Dialogs.EndpointEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="16">
        <TextBlock Text="Name" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />

        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock Text="Method" />
                <ComboBox ItemsSource="{Binding Methods}" SelectedItem="{Binding Method}" MinWidth="110" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Path" />
                <TextBox Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <TextBlock Text="Description" Margin="0,12,0,0" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,4,0,0"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinLines="2" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" MinWidth="80" Margin="0,0,8,0" />
            <Button Content="OK" IsDefault="True" MinWidth="80" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" />
        </StackPanel>
    </StackPanel>
</Window>
