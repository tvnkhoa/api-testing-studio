using System.Windows;

namespace ApiTestingStudio.UI.Views.Dialogs;

/// <summary>Backup settings + restore dialog. Code-behind is view-only (no logic).</summary>
public partial class BackupSettingsDialog : Window
{
    public BackupSettingsDialog() => InitializeComponent();
}
