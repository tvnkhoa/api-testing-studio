<Window x:Class="ApiTestingStudio.UI.Views.Dialogs.VariableEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="16">
        <TextBlock Text="Key" />
        <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />

        <TextBlock Text="Scope" Margin="0,12,0,0" />
        <ComboBox ItemsSource="{Binding Scopes}" SelectedItem="{Binding Scope}" Margin="0,4,0,0" />

        <StackPanel Margin="0,12,0,0">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnvironmentScope}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="Environment" />
            <ComboBox ItemsSource="{Binding Environments}"
                      SelectedItem="{Binding SelectedEnvironment}"
                      DisplayMemberPath="Name"
                      Margin="0,4,0,0" />
        </StackPanel>

        <TextBlock Text="Value" Margin="0,12,0,0" />
        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,4,0,0"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinLines="2" />

        <CheckBox Content="Secret (encrypt value at rest)" IsChecked="{Binding IsSecret}" Margin="0,12,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" MinWidth="80" Margin="0,0,8,0" />
            <Button Content="OK" IsDefault="True" MinWidth="80" IsEnabled="{Binding CanConfirm}" Click="OnConfirm" />
        </StackPanel>
    </StackPanel>
</Window>
